import 'package:flutter/material.dart';

class DartFastButton extends StatelessWidget {
  const DartFastButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
